# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.mypy_cache/
.pytest_cache/
.ruff_cache/
htmlcov/
.coverage

# Node
node_modules/
dist/
*.local

# Env / secrets
.env
*.pem

# OS
.DS_Store

# IDE
.vscode/
.idea/

# Stray directories from a sandbox filesystem quirk hit during git init
# (a corrupted .git/config the sandbox couldn't delete, worked around by
# renaming it aside). Harmless, safe to delete manually outside the sandbox.
.git_broken*/
